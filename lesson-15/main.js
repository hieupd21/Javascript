/*
Làm việc với number
    1. to string
    2. to fixed

*/

var age = 18;
console.log(age);

var PI = Math.PI;
console.log(PI.toFixed(4));

// 1. to string ( toString() )
console.log(age.toString()); // biến number thành string

// 2. to fixed ( toFixed() )
console.log(PI.toFixed(4)); // làm tròn

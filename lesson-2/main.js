/*
Toán tử trong Javascript
    1. Số học
    2. Gán
    3. So sánh
    4. Logic
*/

// Số học
var a = 1 + 2;
console.log('kết quả của số học: ' + a);

// Gán

var name = 'Duy Hiếu';
console.log(`Kết quả của gán chuỗi: ${name}`);

// So sánh

var a = 1;
var b = 2;
if (a > b) {
    console.log('Chuẩn');
} else {
    console.log('Ngáo à');
}

// Logic

var a = 1;
var b = 2;
if (a > 0 || b < 0) {
    console.log('Chạy được không ?');
}